# Ephemeral / machine-local Claude state that lands here when this dir is the
# CLAUDE_CONFIG_DIR of a consumer worktree. Travels with the subtree so every
# consumer ignores it uniformly.
.claude.json
projects/
sessions/
session-env/
shell-snapshots/
history.jsonl
file-history/
cache/
paste-cache/
backups/
ide/
plugins/
mcp-needs-auth-cache.json
.last-cleanup
plans/

# Diagram preview scratch
claude-preview.adoc
memory/diag-*.svg
